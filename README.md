# A solution for the technial assesment

## How to:

1. clone this repository
2. Install dependencies (`npm i` or `yarn` or `pnpm install`)

To run in develpment mode run `npm run dev` or `npm run preview` for production preview.
